package PingPong;

import java.util.ArrayList;

public class Edge {
	/*
	 * 返回各个方向的反弹结果，每个方向要么是1.0，要么-1.0，其中-1.0表示碰到边缘，转向。
	 * The x is ratios of velocities, whose elements are -1.0 or 1.0. When an element is -1.0, it means
	 * the velocity of this direction will be reverse.
	 */
	public ArrayList<Double> touchCollision(ArrayList<Double> x){
		return null;
	}
}
